import type {Appointment,AppointmentStatus} from './mock-data';export const blocking:AppointmentStatus[]=['SCHEDULED','IN_PROGRESS','COMPLETED'];export const forDate=(a:Appointment[],date:string)=>a.filter(x=>x.date===date).sort((a,b)=>a.time.localeCompare(b.time));export const available=(a:Appointment[],date:string,time:string)=>!a.some(x=>x.date===date&&x.time===time&&blocking.includes(x.status));export const suggestions=(a:Appointment[],date:string)=>['08:00','09:00','10:00','11:30','14:00','15:30','16:30'].filter(t=>available(a,date,t)).slice(0,3);
