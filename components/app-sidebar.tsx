'use client';import Link from 'next/link';import {usePathname} from 'next/navigation';import {CalendarPlus,Files,History,Menu,Settings,Users,ChevronDown} from 'lucide-react';import {Avatar,Button} from '@/components/ui/basic';import {useState} from 'react';
const nav=[['Nueva consulta','/new-consultation',CalendarPlus],['Pacientes','/patients',Users],['Historial','#',History],['Plantillas','#',Files],['Configuración','#',Settings]] as const;
export function AppSidebar({children}:{children:React.ReactNode}){const path=usePathname();const [open,setOpen]=useState(false);const Side=()=> <aside className="flex h-full w-72 flex-col border-r bg-background p-4"><h1 className="mb-8 text-xl font-semibold">AgentNotes</h1><nav className="grid gap-1">{nav.map(([n,h,I])=><Link key={n} href={h} className={`flex items-center gap-3 rounded-md px-3 py-2 text-sm ${path===h?'bg-primary text-primary-foreground':'hover:bg-muted'}`}><I className="h-4 w-4"/>{n}</Link>)}</nav><div className="mt-auto flex items-center gap-3 rounded-lg border p-3"><Avatar initials="AT"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">Dr. Alejandro Torres</p><p className="text-xs text-muted-foreground">Workspace personal</p></div><button aria-label="Menú de usuario"><ChevronDown className="h-4 w-4"/></button></div></aside>;return <div className="min-h-screen md:flex"><div className="hidden md:block"><Side/></div><div className="border-b p-3 md:hidden"><Button variant="outline" size="icon" onClick={()=>setOpen(true)} aria-label="Abrir navegación"><Menu className="h-4 w-4"/></Button></div>{open&&<div className="fixed inset-0 z-50 bg-black/30" onClick={()=>setOpen(false)}><div onClick={e=>e.stopPropagation()}><Side/></div></div>}<main className="flex-1 overflow-hidden">{children}</main></div>}
